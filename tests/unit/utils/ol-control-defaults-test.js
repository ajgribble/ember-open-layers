import olControlDefaults from 'dummy/utils/ol-control-defaults';
import { module, test } from 'qunit';

module('Unit | Utility | ol-control-defaults', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = olControlDefaults();
    assert.ok(result);
  });
});
