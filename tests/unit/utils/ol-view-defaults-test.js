import olViewDefaults from 'dummy/utils/ol-view-defaults';
import { module, test } from 'qunit';

module('Unit | Utility | ol-view-defaults', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = olViewDefaults();
    assert.ok(result);
  });
});
